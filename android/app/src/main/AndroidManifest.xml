<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yogicmile.app">

    <!-- =========================== -->
    <!-- PERMISSIONS FOR STEP TRACKING -->
    <!-- =========================== -->
    
    <!-- Android 12+ - Physical Activity (step counter) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.BODY_SENSORS_BACKGROUND" />

    <!-- General permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional: For GPS tracking (if implementing route tracking) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- For background step tracking service -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />

    <!-- Optional: Push notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Optional: Camera for verification photos -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Feature declarations (what your app needs to work) -->
    <uses-feature
        android:name="android.hardware.sensor.accelerometer"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="false" />

    <!-- Application declaration -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep linking for auth redirect -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="yogicmile"
                    android:host="auth-callback" />
                <data
                    android:scheme="yogicmile"
                    android:host="auth-success" />
            </intent-filter>

            <!-- Deep linking for email verification (optional) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="app.lovable.dev"
                    android:path="/auth/redirect" />
            </intent-filter>
        </activity>

        <!-- Background Service for Step Tracking -->
        <service
            android:name=".services.StepTrackingService"
            android:exported="false"
            android:label="Yogic Mile Step Tracking Service">
        </service>

        <!-- Background Job Scheduler for periodic sync -->
        <service
            android:name=".services.StepSyncJobService"
            android:exported="true"
            android:permission="android.permission.BIND_JOB_SERVICE">
        </service>

        <!-- Broadcast receiver for boot completion (start service on device boot) -->
        <receiver
            android:name=".receivers.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- App shortcuts (optional) -->
        <meta-data
            android:name="android.app.shortcuts"
            android:resource="@xml/shortcuts" />

    </application>

</manifest>
```

## capacitor.config.ts - FIXED Configuration

```typescript
// Location: capacitor.config.ts

import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yogicmile.app',
  appName: 'Yogic Mile',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
    cleartext: false,
  },
  
  // Android-specific configuration
  android: {
    path: 'android',
    minWebViewVersion: 90,
    allowMixedContent: false,
  },

  // iOS-specific configuration
  ios: {
    path: 'ios',
    minVersion: '12.0',
  },

  // Plugin configurations
  plugins: {
    // Capacitor core
    'CapacitorCore': {
      // Enable permission delegation
      allowMixedContent: false,
    },

    // Permissions plugin
    'Permissions': {
      // Permissions configuration
      'BodySensors': {
        'requestAsyn': true,
      },
    },

    // Storage plugin for persistent data
    'Storage': {
      'autoInit': true,
    },

    // App lifecycle
    'App': {
      'name': 'Yogic Mile',
    },

    // Notifications (if using push notifications)
    'PushNotifications': {
      'presentationOptions': ['badge', 'sound', 'alert'],
    },

    // Status bar
    'StatusBar': {
      'style': 'dark',
      'backgroundColor': '#ffffff',
      'overlaysWebView': false,
    },

    // Safe area
    'SafeArea': {
      'customSafeAreaInsets': {
        'top': 0,
        'bottom': 0,
        'left': 0,
        'right': 0,
      },
    },
  },
};

export default config;
