import React from 'react';
import { SupportTestingSuite } from '@/components/support/SupportTestingSuite';

export const SupportTestPage: React.FC = () => {
  return <SupportTestingSuite />;
};

export default SupportTestPage;